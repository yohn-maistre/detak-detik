---
import Paper from '../layouts/Paper.astro';
import Loader from '../components/Loader.astro';
import PetaKabar from '../islands/PetaKabar.svelte';
import NasionalPagi from '../islands/NasionalPagi.svelte';
import CuacaPagi from '../islands/CuacaPagi.svelte';
import PasarPagi from '../islands/PasarPagi.svelte';
import GradienKeadilan from '../islands/GradienKeadilan.svelte';
import Gelombang from '../islands/Gelombang.svelte';
import SensusDiri from '../islands/SensusDiri.svelte';
import TebakDaerah from '../islands/TebakDaerah.svelte';
import BenarSalah from '../islands/BenarSalah.svelte';
import Aksara from '../islands/Aksara.svelte';
import AlmanakSains from '../islands/AlmanakSains.svelte';
import RimbaHidup from '../islands/RimbaHidup.svelte';
import RepublikOligarki from '../islands/RepublikOligarki.svelte';
import GaleriNusantara from '../islands/GaleriNusantara.svelte';
import WajahNusantara from '../islands/WajahNusantara.svelte';
import ZonaHayati from '../islands/ZonaHayati.svelte';
import PustakaPikiran from '../islands/PustakaPikiran.svelte';
import TulisanPapan from '../islands/TulisanPapan.svelte';
import YangBerubah from '../islands/YangBerubah.svelte';
import IngatanHari from '../islands/IngatanHari.svelte';
import Bahasa718 from '../islands/Bahasa718.svelte';
import KasDaerah from '../islands/KasDaerah.svelte';
import HutanHilang from '../islands/HutanHilang.svelte';
import TuguRakyat from '../islands/TuguRakyat.svelte';
import LensaWilayah from '../islands/LensaWilayah.svelte';
import CabangBand from '../islands/CabangBand.svelte';
import SkorCabang from '../islands/SkorCabang.svelte';
import { CABANG } from '../lib/data/cabang';
import PabrikUU from '../islands/PabrikUU.svelte';
// GarisStart & DuaGaris retired to the arsip-perspektif shelf (permalinks
// only); the seeded JciVsPeers & RupiahIHSG were deleted (wave 9b).
import FloatKonsentrasi from '../islands/FloatKonsentrasi.svelte';
import KabinetWaffle from '../islands/KabinetWaffle.svelte';
import AgendaIstana from '../islands/AgendaIstana.svelte';
import PartaiPapan from '../islands/PartaiPapan.svelte';
import HariIniNegara from '../islands/HariIniNegara.svelte';
import SuaraLembaga from '../islands/SuaraLembaga.svelte';
import LembaranNegara from '../islands/LembaranNegara.svelte';
import PetaAtlas from '../islands/PetaAtlas.svelte';
import EdisiFeed from '../islands/EdisiFeed.svelte';
import DaftarIsi from '../islands/DaftarIsi.svelte';
import MbgKorban from '../islands/MbgKorban.svelte';
import ObituariHutan from '../components/ObituariHutan.astro';
import { EDISI, ANGKA_EDISI, TICKER, TEMUAN, STRUK, KEHENINGAN, PUTUSAN_HARIAN } from '../lib/data/edisi';
// aparat record: one owner for the band's 602, the funnel, and these rows
import { APARAT_ANGGARAN, APARAT_KEKERASAN } from '../lib/data/akuntabilitas';
// the retired kartu vital's budget-share fact keeps its owner (vital_cabang.json)
import VITAL_CABANG from '../../newsroom/data/vital_cabang.json';
const APARAT_PORSI = VITAL_CABANG.find((v) => v.id === 'aparat-anggaran');
// the buku besar: the SAME registry the newsroom's janji desk audits every
// edition (single source of truth — see newsroom/sources/janji.py)
import JANJI from '../../newsroom/data/janji_registry.json';
import { PIECES } from '../lib/perspektif';

const fmtRp = new Intl.NumberFormat('id-ID');

// janji dumbbell from the registry's EXPLICIT figures (target_angka /
// realisasi_angka — no string parsing): a full pair draws a track (target
// tick vs measured dot), a missing figure draws the absence itself
// (hatched track), never a fabricated position
const JANJI_TRACK = JANJI.map((j) => {
  const t = j.target_angka;
  const r = j.realisasi_angka;
  if (t == null || r == null) return null;
  const maks = Math.max(t, r, 0.0001) * 1.25;
  return { t: (t / maks) * 100, r: (r / maks) * 100 };
});
const oligarki = PIECES.find((p) => p.slug === 'republik-oligarki');
const pabrik = PIECES.find((p) => p.slug === 'pabrik-undang-undang');

// The world scoreboard: where the republic ranks, moved into Act II beside the
// Vietnam comparison. Big numerals, peer context, ruled rows (not tiles).
const DUNIA = [
  { nilai: '№2', label: 'Negara paling kaya hayati di dunia', peer: 'di belakang Brasil', chip: 'cbd · megadiverse', baik: true },
  { nilai: '718', label: 'Bahasa daerah yang masih hidup', peer: 'terbanyak kedua setelah Papua Nugini', chip: 'badan bahasa', baik: true },
  { nilai: '76%', label: 'Spesies karang dunia ada di perairan kita', peer: 'jantung Coral Triangle', chip: 'coral triangle', baik: true },
  { nilai: '129', label: 'Peringkat kebebasan pers dari 180 negara', peer: 'turun dari 111 pada 2024', chip: 'rsf 2026', baik: false },
  { nilai: '5,62', label: 'Skor kebahagiaan dunia', peer: 'di bawah tetangga ASEAN', chip: 'whr 2026', baik: false },
  { nilai: '№1', label: 'Mamalia terancam punah terbanyak di dunia', peer: '±191 spesies', chip: 'iucn 2025', baik: false },
];

const fmtAngka = new Intl.NumberFormat('id-ID').format(ANGKA_EDISI.nilai);
const sparkMax = Math.max(...PUTUSAN_HARIAN);
const sparkTotal = PUTUSAN_HARIAN.reduce((a, b) => a + b, 0);

// build-time live-lane check: is PUBLIC_AKSARA_URL baked into this build?
const AKSARA_BUILD = import.meta.env.PUBLIC_AKSARA_URL as string | undefined;
let lajurHost: string | null = null;
if (AKSARA_BUILD) { try { lajurHost = new URL(AKSARA_BUILD).host; } catch { lajurHost = AKSARA_BUILD; } }

---

<Paper>
  <Loader />

  <!-- tarik fajar: the overscroll dawn, opened by pulling past the top -->
  <div id="fajar" aria-hidden="true">
    <canvas></canvas>
    <p class="fajar-label mono">EDISI PAGI · MATAHARI DICETAK 05.00 WIB</p>
  </div>

  <main data-stempel-pad>
    <!-- ============ ACT I · DINAS ============ -->
    <section class="act act-dinas" data-act="dinas" data-register="dinas" id="depan">
      <div class="tex tex-scan" aria-hidden="true"></div>
      <div class="wrap">
        <header class="masthead-block">
          <div class="folio-top mono">
            <span>{EDISI.serial}</span>
            <span class="jam-tri" id="jam-tri" aria-label="Waktu Indonesia tiga zona">
              <span class="detik" aria-hidden="true" title="detik demi detik">
                <svg viewBox="0 0 40 40">
                  <circle cx="20" cy="20" r="18.5" class="detik-rim" />
                  {Array.from({ length: 60 }).map((_, i) => (
                    <line x1="20" y1="2.4" x2="20" y2={i % 5 === 0 ? 4.8 : 3.8} class={i % 5 === 0 ? 'detik-maj' : 'detik-min'} transform={`rotate(${i * 6} 20 20)`} />
                  ))}
                  <line x1="20" y1="20.5" x2="20" y2="5.5" class="detik-hand" />
                  <circle cx="20" cy="20" r="1.5" class="detik-hub" />
                </svg>
              </span>
              <span class="jam-z"><b data-jam="wib">--.--.--</b> WIB</span>
              <span class="jam-z"><b data-jam="wita">--.--.--</b> WITA</span>
              <span class="jam-z"><b data-jam="wit">--.--.--</b> WIT</span>
            </span>
          </div>

          <div class="masthead-grid">
            <h1 class="masthead display" id="masthead">
              <span class="mast-line">DETAK</span>
              <span class="mast-line">DETIK</span>
            </h1>
            <aside class="infobox mono" aria-label="Kotak edisi">
              <div class="infobox-row"><span>EDISI</span><b class="num">#{EDISI.nomor}</b></div>
              <div class="infobox-row"><span>PEMBARUAN</span><b>2× SEHARI</b></div>
              <div class="infobox-row"><span>HARGA</span><b>GRATIS</b></div>
              <div class="infobox-row"><span>OPLAH</span><b>SELURUH REPUBLIK</b></div>
              <div class="infobox-row"><span>LAJUR</span>{AKSARA_BUILD
                ? <b class="lajur-live">● LANGSUNG</b>
                : <b class="lajur-contoh">○ CONTOH</b>}</div>
              <div class="barcode" aria-hidden="true"></div>
              <p class="infobox-foot">DICETAK MESIN ·<br />DIPERIKSA HUKUM</p>
            </aside>
          </div>

          <div class="folio mono">
            <span>{EDISI.tanggal}</span>
            <span>Dicetak otomatis <b>{EDISI.dicetak}</b></span>
            <span>Terbuka · Bercatatan · Tanpa pelacak</span>
            <button class="fajar-btn mono" data-fajar-open aria-expanded="false" aria-label="Buka fajar">◐ FAJAR</button>
          </div>
          <div class="rule2" aria-hidden="true"></div>
        </header>

        <div class="ticker" aria-label="Berita kilat dari media lain, tautan keluar apa adanya">
          <span class="ticker-tab"><i class="pulse-dot"></i>KILAS</span>
          <div class="ticker-belt">
            <div class="ticker-track" id="ticker-track">
              {TICKER.map((t) => (
                <Fragment>
                  <a class="ticker-link" href={t.url} target="_blank" rel="noopener"><span class="src">{t.src}</span> {t.teks}</a> <span class="sep">●</span>
                </Fragment>
              ))}
              {TICKER.map((t) => (
                <Fragment>
                  <a class="ticker-link" href={t.url} target="_blank" rel="noopener"><span class="src">{t.src}</span> {t.teks}</a> <span class="sep">●</span>
                </Fragment>
              ))}
            </div>
          </div>
        </div>

        <!-- ===== HALAMAN MUKA: one feed. Entry №01 is the paper's own lead
             (SSR'd contoh, swapped live by the island); the day's press
             follows as ranked kliping rows. Headlines open the Lembar. -->
        <EdisiFeed
          client:load
          lead={{
            headline: TEMUAN[0].headline,
            dek: TEMUAN[0].body,
            chips: TEMUAN[0].chips,
            stamp: TEMUAN[0].stamp,
            serial: TEMUAN[0].serial,
          }}
        />

        <!-- INGATAN REDAKSI, the reader's face: what changed vs the arsip
             window — prints only when the edition carries memory -->
        <YangBerubah client:visible />

        <!-- the republic, mapped: right under the news, the country it
             happens in. Live layers + one province at a time. -->
        <section class="peta-blok" id="peta-blok" aria-label="Peta Kabar">
          <div class="kabar-head">
            <span class="inkbar"><span class="dot">●</span>PETA KABAR</span>
            <span class="eyebrow">LAPISAN DATA LANGSUNG · KLIK SEBUAH PROVINSI ATAU TITIK, ATAU MINTA AKSARA MENELUSURINYA</span>
          </div>
          <div class="peta-cuaca">
            <CuacaPagi client:visible />
          </div>
          <PetaKabar client:visible />
        </section>

        <!-- PASAR: the macro board, then the kitchen-table series — one section -->
        <section class="pasar-blok" id="pasar" aria-label="Pasar">
          <PasarPagi client:visible />
          <div class="gelombang-blok" data-rise>
            <Gelombang client:visible />
          </div>
        </section>

      </div>
    </section>

    <!-- ============ SEAM I · siang turun ============ -->
    <div class="seam" data-seam="dinas-mesin" style="--from:#d6cbac;--to:#100f0d" aria-hidden="true">
      <div class="slats">
        {Array.from({ length: 8 }).map(() => (
          <div class="slat"><i class="f"></i><i class="b"></i></div>
        ))}
      </div>
      <p class="seam-kicker mono">↓ MALAM · YANG TAK INGIN DICATAT, TETAP DICATAT</p>
    </div>

    <!-- ============ ACT II · MESIN ============ -->
    <section class="act act-mesin" data-act="mesin" data-register="mesin" id="mesin">
      <div class="wrap">
        <!-- Angka Edisi -->
        <section class="angka-blok" aria-labelledby="angka-label">
          <div class="angka-kiri">
            <span class="inkbar"><span class="dot">●</span>PAPAN ANGKA · ANGKA EDISI</span>
            <p class="angka num display" id="angka-odometer" data-odometer={ANGKA_EDISI.nilai} data-prefix={ANGKA_EDISI.prefix} title="Tarik mendatar untuk memutar ulang angka">
              {ANGKA_EDISI.prefix} {fmtAngka}
            </p>
            <p class="angka-label" id="angka-label">
              <span id="angka-label-teks">{ANGKA_EDISI.label}</span> <span class="mono" id="angka-label-src">(data contoh)</span>
            </p>
            <div class="angka-denom mono" role="group" aria-label="Setarakan kerugian dengan satuan lain">
              <span class="angka-denom-label">SETARA DENGAN</span>
              <button class="angka-denom-btn aktif" data-denom="rp">RUPIAH</button>
              <button class="angka-denom-btn" data-denom="pertamax">LITER PERTAMAX</button>
              <button class="angka-denom-btn" data-denom="mbg">PORSI MBG</button>
              <button class="angka-denom-btn" data-denom="umphari">HARI UPAH MINIMUM</button>
              <button class="angka-denom-btn" data-denom="starship">PELUNCURAN STARSHIP</button>
            </div>
            <div class="cite-chips">
              {ANGKA_EDISI.chips.map((c) => <button class="chip"><span class="tick">⊙</span>{c}</button>)}
              <button class="chip hop">Jadikan tur →</button>
            </div>
          </div>
          <div class="angka-sela" aria-hidden="true"></div>
          <div class="angka-kanan">
            <span class="eyebrow">PUTUSAN MASUK · 14 HARI TERAKHIR</span>
            <div class="sparkbars" role="img" aria-label={`Diagram batang ${sparkTotal} putusan dalam 14 hari`}>
              {PUTUSAN_HARIAN.map((v) => (
                <i class="sparkbar" style={`--h:${Math.round((v / sparkMax) * 100)}%`} data-v={v}></i>
              ))}
            </div>
            <p class="spark-foot mono"><b class="num">{sparkTotal}</b> PUTUSAN BERKEKUATAN TETAP · <span class="spark-max num">PUNCAK {sparkMax}/HARI</span></p>
          </div>
        </section>

        <!-- the state of the nation: live counters + macro gauges, each read
             against its stated target. Vitals belong to the machine, so this
             prints right after the loss counter (moved from Act I). -->
        <section class="negara-blok" id="negara">
          <div class="kabar-head">
            <span class="inkbar"><span class="dot">●</span>NEGARA HARI INI</span>
            <span class="eyebrow">PENGHITUNG LANGSUNG · ANGKA MAKRO TERHADAP SASARANNYA · METER HARIAN</span>
          </div>
          <NasionalPagi client:visible />
        </section>

        <!-- Temuan Redaksi: the ranked findings board. SSR renders the contoh
             set; pagi-live rebuilds it from the published edition when live. -->
        <section class="temuan-blok" aria-label="Temuan redaksi edisi ini" data-rise>
          <div class="temuan-head-row">
            <span class="inkbar"><span class="dot">●</span>TEMUAN REDAKSI</span>
            <span class="eyebrow">DERET TEMUAN LOLOS PERIKSA FAKTA · <span id="temuan-src" class="mono">DATA CONTOH</span></span>
          </div>
          <div class="temuan-grid" id="temuan-grid">
            {TEMUAN.slice(0, 3).map((t, i) => (
              <article class="temuan">
                <span class="ghost-num num" aria-hidden="true">{String(i + 1).padStart(2, '0')}</span>
                <div class="temuan-head"><span class="temuan-lens mono">{t.lens}</span><span class="mono">{t.serial}</span></div>
                <h3 class="temuan-headline display">{t.headline}</h3>
                <p class="temuan-body">{t.body}</p>
                <div class="temuan-foot"><div class="cite-chips"><button class="chip"><span class="tick">⊙</span>{t.id}</button></div></div>
              </article>
            ))}
          </div>
        </section>

        <!-- ╌╌ GERAK I · LIMA CABANG KEKUASAAN ╌╌ -->
        <div class="gerak" data-rise>
          <span class="gerak-no mono">GERAK I</span>
          <h2 class="gerak-judul display">Lima Cabang Kekuasaan</h2>
          <p class="gerak-dek fig">Siapa yang memegang kuasa di republik ini, dan bagaimana ia memakainya.</p>
        </div>

        <!-- the five branches, as a slim index; each band now leads its chapter -->
        <nav class="cabang-indeks" aria-label="Lima cabang kekuasaan">
          <span class="ci-head mono"><span class="dot">●</span>LIMA CABANG KEKUASAAN</span>
          <ol class="ci-list">
            {CABANG.map((c) => (
              <li class="ci-item">
                <a href={`#${c.anchor}`}>
                  <span class="ci-no mono">{c.no}</span>
                  <span class="ci-nama display">{c.nama}</span>
                  <span class="ci-ringkas">{c.ringkas}</span>
                  <span class="ci-panah" aria-hidden="true">↓</span>
                </a>
              </li>
            ))}
          </ol>
        </nav>

        {/* the aggregator's front desk (§13.19): the newest documented act
             per pantau lane — the reason to come back every morning */}
        <section class="hari-ini" aria-labelledby="hn-judul">
          <span class="eyebrow" id="hn-judul">HARI INI DI NEGARA · CATATAN TERBARU TIAP LAJUR PANTAU</span>
          <HariIniNegara client:visible />
        </section>

        <!-- CABANG · EKSEKUTIF -->
        <section class="bab" id="kuasa">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">01</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>EKSEKUTIF</span>
              <h2 class="bab-tajuk display">Presiden &amp; Kabinet</h2>
              <p class="bab-fungsi"><span class="fig">&ldquo;Presiden Republik Indonesia memegang kekuasaan pemerintahan menurut Undang-Undang Dasar.&rdquo;</span> <span class="mono">UUD 1945 · PS. 4 (1)</span></p>
              <p class="bab-dek">Kabinet terbesar sejak 1966; apa yang dikerjakannya tercatat di agenda resminya sendiri.</p>
            </div>
          </header>
          <CabangBand cabang="eksekutif" client:visible />
          <!-- kartu vital retired 2026-07-12 (Yose: one fact family, one
               surface) — its like-for-like count + receipt live in the band -->


          <!-- WHAT/WHERE/FOR WHOM: the office's own published record, grown
               twice daily by the keyless setkab harvest (sources/agenda.py) -->
          <div class="bab-lembar" data-rise>
            <h3 class="bab-subjudul display">Agenda Istana</h3>
            <p class="bab-kalimat fig">Apa yang presiden kerjakan, di mana, untuk siapa — dibaca dari terbitan resmi Sekretariat Kabinet, baris demi baris, dengan tautan ke dokumen aslinya.</p>
            <AgendaIstana client:visible />
          </div>

          <SkorCabang cabang="eksekutif" client:visible />
          <div class="bab-lembar" data-rise>
            <h3 class="bab-subjudul display">Kabinet yang Membengkak</h3>
            <p class="bab-kalimat fig">Satu sel tiap pejabat, tersusun menurut tingkatnya — dan di sampingnya jam Danantara terus berjalan, menunggu laporan keuangan pertama.</p>
            <KabinetWaffle client:visible />
          </div>
        </section>

        <!-- CABANG · LEGISLATIF -->
        <section class="bab" id="pabrik">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">02</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>LEGISLATIF</span>
              <h2 class="bab-tajuk display">Wakil &amp; Undang-Undang</h2>
              <p class="bab-fungsi"><span class="fig">&ldquo;Dewan Perwakilan Rakyat memegang kekuasaan membentuk undang-undang.&rdquo;</span> <span class="mono">UUD 1945 · PS. 20 (1)</span></p>
              <p class="bab-dek">DPR 580 kursi; kecepatan mengesahkan RUU menunjukkan prioritasnya.</p>
            </div>
          </header>
          <CabangBand cabang="legislatif" client:visible />
          <SkorCabang cabang="legislatif" client:visible />
          {pabrik && (
            <div class="bab-lembar" data-rise>
              <h3 class="bab-judul display">{pabrik.judul}</h3>
              <p class="bab-kalimat fig">{pabrik.satuKalimat}</p>
              <PabrikUU client:visible />
            </div>
          )}

          {/* the three voices (§13.19): above, how the machine performs;
               here, what became LAW (ink) and what the chamber SAYS (voice) */}
          <div class="bab-lembar bab-sub" data-rise>
            <LembaranNegara client:visible />
          </div>
          <div class="bab-lembar bab-sub" data-rise>
            <SuaraLembaga cabang="legislatif" client:visible />
          </div>
        </section>

        <!-- CABANG · YUDIKATIF -->
        <section class="bab" id="hukum">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">03</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>YUDIKATIF</span>
              <h2 class="bab-tajuk display">Hukum &amp; Vonis</h2>
              <p class="bab-fungsi"><span class="fig">&ldquo;Kekuasaan kehakiman merupakan kekuasaan yang merdeka untuk menyelenggarakan peradilan guna menegakkan hukum dan keadilan.&rdquo;</span> <span class="mono">UUD 1945 · PS. 24 (1)</span></p>
              <p class="bab-dek">{TEMUAN[0].headline}. <span class="mono">(data contoh)</span></p>
            </div>
          </header>
          <CabangBand cabang="yudikatif" client:visible />
          <SkorCabang cabang="yudikatif" client:visible />
          <div class="bab-lembar" data-rise>
            <h3 class="bab-subjudul display">Hukuman yang Tak Sebanding</h3>
            <p class="bab-kalimat fig">Besar kerugian negara terhadap lama vonis, satu titik per putusan; jaraknya yang melebar adalah temuannya.</p>
            <div class="hukum-grid">
              <GradienKeadilan client:visible />
              <div class="struk-rail">
                <p class="figcap-dinas mono">SATU PUTUSAN, DIPERIKSA:</p>
                {TEMUAN[0].chips.map((c) => <button class="chip"><span class="tick">⊙</span>{c}</button>)}
                <span class="stamp" data-stamp-in>{TEMUAN[0].stamp}</span>
              </div>
            </div>
          </div>

          <!-- absence is content: the branch whose records sit behind
               challenge walls says so in print — one ruled line, not a section -->
          <div class="ledger lingk-absen absen-satu">
            <div><span><b class="absen-k mono">YANG BELUM DICETAK · MENUNGGU KURASI</b> Putusan langsung dari Direktori MA <span class="mono">portal berlapis tantangan untuk mesin — kurasi manual berjalan (ICW Tren Vonis)</span></span><span class="v num">—</span></div>
          </div>
        </section>

        <!-- BAB III · APARAT -->
        <section class="bab" id="aparat">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">04</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>APARAT</span>
              <h2 class="bab-tajuk display">Alat Paksa Negara</h2>
              <p class="bab-fungsi"><span class="fig">&ldquo;&hellip; bertugas melindungi, mengayomi, melayani masyarakat, serta menegakkan hukum.&rdquo;</span> <span class="mono">UUD 1945 · PS. 30 (4), TENTANG POLRI</span></p>
              <p class="bab-dek">Anggaran terbesar negara, dan pertanggungjawaban yang paling jarang sampai ke pengadilan.</p>
            </div>
          </header>
          <CabangBand cabang="aparat" client:visible />
          <SkorCabang cabang="aparat" client:visible />
          <div class="aparat-grid">
            <div class="aparat-col" data-rise>
              <span class="eyebrow">ANGGARAN 2026</span>
              {APARAT_ANGGARAN.map((a) => (
                <div class="aparat-row mono"><span>{a.k}</span><b class="num">{a.v}</b></div>
              ))}
              {APARAT_PORSI && (
                <div class="aparat-row mono"><span>{APARAT_PORSI.metrik}</span><b class="num">{APARAT_PORSI.nilai.toLocaleString('id-ID')}%</b></div>
              )}
              <p class="aparat-note">Dua pos terbesar dalam APBN.{APARAT_PORSI ? ` ${APARAT_PORSI.catatan}.` : ''}</p>
              <button class="chip"><span class="tick">⊙</span>apbn 2026 · {APARAT_PORSI?.band.sumber ?? ''}</button>
            </div>
            <!-- the 602 total is the chapter's signature figure (band above);
                 this column holds the breakdown it alone carries -->
            <div class="aparat-col besar" data-rise>
              <span class="eyebrow">KEKERASAN POLISI · {APARAT_KEKERASAN.periode} · RINCIAN</span>
              <div class="aparat-row mono"><span>Insiden dengan tembakan</span><b class="num">{APARAT_KEKERASAN.tembak}</b></div>
              <div class="aparat-row mono"><span>Korban tewas</span><b class="num">{APARAT_KEKERASAN.tewas}</b></div>
              <div class="aparat-row mono"><span data-annotate="underline" data-annotate-color="#e44a06">Perwira aktif di jabatan sipil</span><b class="num">{fmtRp.format(APARAT_KEKERASAN.perwiraSipil)}</b></div>
              <p class="aparat-impun">{APARAT_KEKERASAN.impunitas}</p>
              <p class="figcap-dinas mono">KADENS PEMBARUAN: TAHUNAN (CATAHU KONTRAS, TIAP JULI) — BUKAN UMPAN HARIAN</p>
              <button class="chip"><span class="tick">⊙</span>{APARAT_KEKERASAN.sumber} · imparsial</button>
            </div>
          </div>

          {/* the institution's own account, labeled as such — beside the
               independent record above, never blended with it */}
          <div class="bab-lembar bab-sub" data-rise>
            <SuaraLembaga cabang="aparat" client:visible />
          </div>
        </section>

        <!-- CABANG · DAERAH -->
        <section class="bab" id="daerah">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">05</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>DAERAH</span>
              <h2 class="bab-tajuk display">Pemerintahan &amp; Anggaran</h2>
              <p class="bab-fungsi"><span class="fig">&ldquo;Pemerintahan daerah menjalankan otonomi seluas-luasnya &hellip;&rdquo;</span> <span class="mono">UUD 1945 · PS. 18 (5)</span></p>
              <p class="bab-dek">{TEMUAN[2].headline}. <span class="mono">(data contoh)</span></p>
            </div>
          </header>
          <CabangBand cabang="daerah" client:visible />
          <SkorCabang cabang="daerah" client:visible />

          <!-- the chapter's instrument: the morphing region lens, moved here
               from Act I. The map upstairs still files into it via the bus. -->
          <div class="bab-lembar daerah-lensa" data-rise>
            <h3 class="bab-subjudul display">Lensa Wilayah</h3>
            <p class="bab-kalimat fig">Cari provinsi atau ketuk peta di bagian Pagi; angka nasional bertukar menjadi angka daerah itu, dengan peringkatnya di antara 38 provinsi.</p>
            <LensaWilayah client:visible />
          </div>

          <div class="hukum-grid">
            <div class="ekonomi-col" data-rise>
              <!-- the APBN total prints once, on the struk beside this column:
                   the artifact owns it (satu fakta satu pemilik) -->
              <div class="ekonomi-otsus">
                <span class="eyebrow">KAS DAERAH · BELANJA PEGAWAI 546 PEMDA TERHADAP PLAFON UU</span>
                <!-- follows the lens: any prov/kab picked upstairs is named on
                     the strip; a Papua province also opens the otsus exhibit -->
                <KasDaerah client:visible />
              </div>
            </div>
            <div class="struk-rail">
              <div class="struk" data-rise>
                <span class="stamp struk-stamp" data-stamp-in>{STRUK.stamp}</span>
                <h4 class="mono">{STRUK.judul}</h4>
                <p class="struk-sub mono">{STRUK.sub}</p>
                <div class="dash"></div>
                {STRUK.baris.map((b) => (
                  <div class="row mono"><span>{b.k}</span><span class="num">{b.v}</span></div>
                ))}
                <div class="dash"></div>
                {STRUK.total.map((b) => (
                  <div class="row tot mono"><span>{b.k}</span><span class="num">{b.v}</span></div>
                ))}
                <div class="dash"></div>
                <p class="roast" data-annotate="box" data-annotate-color="#e44a06">{STRUK.roast}</p>
              </div>
              <p class="figcap-dinas mono">ARTEFAK · STRUK BELANJA NEGARA · TARIK KE BAWAH UNTUK MEROBEK</p>
            </div>
          </div>

        </section>

        {/* BAB 06 · PARTAI & KEPENTINGAN — GERAK I's closing chapter: the
             interests that operate across all five branches. §4.4 law:
             documents only — kursi, jabatan, kepemilikan; never a score. */}
        <section class="bab" id="partai">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">06</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>PARTAI &amp; KEPENTINGAN</span>
              <h2 class="bab-tajuk display">Siapa Memegang Apa</h2>
              <p class="bab-dek">Kursi di parlemen, gubernur di daerah, grup media di tangan pengurusnya — dokumen saja, tanpa skor. Supaya jelas ke pintu siapa sebuah urusan pantas diketuk.</p>
            </div>
          </header>
          <PartaiPapan client:visible />
        </section>

        <!-- BAB 07 · JANJI & PROGRAM: the ledger that remembers, and the programs
             it grades — closes GERAK I: what the officeholders promised sits
             right after who they are. (Moved out of Keadaan Negeri, 2026-07-11;
             the TenggatStrip teaser died with the move — one ledger, one owner.) -->
        <section class="bab" id="janji">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">07</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>BUKU BESAR · JANJI</span>
              <h2 class="bab-tajuk display">Janji &amp; Program</h2>
              <p class="bab-dek">Buku besar yang mengingat: target yang diucapkan, angka yang terukur, dan ongkos program terbesarnya. Dinilai dengan aritmetika, bukan opini.</p>
            </div>
          </header>

          <!-- the buku besar itself: numbered ruled entries, promise beside its
               measurement, stamped mechanically from deadline × figure -->
          <div class="janji-buku">
            {JANJI.map((j, ji) => (
              <article class="janji">
                <span class="janji-no ghost-num num" aria-hidden="true">{String(ji + 1).padStart(2, '0')}</span>
                <div class="janji-isi">
                  <p class="janji-teks">{j.teks}</p>
                  <p class="janji-meta mono">{j.sumber}</p>
                </div>
                <div class="janji-ukur">
                  <div class="janji-baris mono">
                    <span>TARGET · {j.target}</span>
                    <span>TERUKUR · {j.realisasi}</span>
                  </div>
                  {JANJI_TRACK[ji] ? (
                    <div class={`janji-track ${j.status === 'TIDAK TERCAPAI' ? 'gagal' : ''}`} role="img" aria-label={`Target ${j.target}, terukur ${j.realisasi}`}>
                      <i class="jt-target" style={`left:${JANJI_TRACK[ji].t}%`}></i>
                      <i class="jt-bar" style={`left:${Math.min(JANJI_TRACK[ji].t, JANJI_TRACK[ji].r)}%;width:${Math.abs(JANJI_TRACK[ji].t - JANJI_TRACK[ji].r)}%`}></i>
                      <i class="jt-real" style={`left:${JANJI_TRACK[ji].r}%`}></i>
                    </div>
                  ) : (
                    <div class="janji-track kosong" role="img" aria-label="Realisasi tidak tersedia"></div>
                  )}
                </div>
                <span class={`stamp janji-stamp s-${j.status.replaceAll(' ', '-').toLowerCase()}`} data-stamp-in>{j.status}</span>
              </article>
            ))}
          </div>
          <p class="janji-foot mono">STATUS DIHITUNG DARI TENGGAT × ANGKA TERUKUR · SUMBER TIAP BARIS TERCATAT · ▌TARGET ● TERUKUR</p>

          <div class="bab-lembar bab-sub" data-rise>
            <h3 class="bab-subjudul display">Catatan Keracunan MBG</h3>
            <p class="bab-kalimat fig">Makan Bergizi Gratis: anggaran ratusan triliun, dan insiden keracunan yang tercatat — disajikan sebagai catatan, menunggu rekap sumber resmi.</p>
            <MbgKorban client:visible />
          </div>
        </section>

        <!-- ╌╌ GERAK II · KEADAAN NEGERI ╌╌ -->
        <div class="gerak" data-rise>
          <span class="gerak-no mono">GERAK II</span>
          <h2 class="gerak-judul display">Keadaan Negeri</h2>
          <p class="gerak-dek fig">Bukan lembaga, melainkan keadaan yang ditanggung bersama: siapa memiliki apa, pasar, posisi di antara bangsa, dan sumber daya alam.</p>
        </div>

        <!-- RAK 07 · KEPEMILIKAN — one header, one instrument (rack grammar) -->
        {oligarki && (
          <section class="bab" id="oligarki">
            <header class="bab-kepala">
              <span class="bab-folio ghost-num" aria-hidden="true">08</span>
              <div class="bab-kepala-teks">
                <span class="inkbar"><span class="dot">●</span>TEMA · KEPEMILIKAN</span>
                <h2 class="bab-tajuk display">{oligarki.judul}</h2>
                <p class="bab-dek">{oligarki.satuKalimat}</p>
              </div>
            </header>
            <RepublikOligarki client:visible />
          </section>
        )}

        {/* RAK 08 · PASAR — the float ruler; measured-from-outside series
             print from real rows or not at all (two seeded charts died
             here, wave 9b — real rows or silence) */}
        <section class="bab" id="ekonomi">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">09</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>TEMA · PASAR</span>
              <h2 class="bab-tajuk display">Pasar yang Tipis</h2>
              <p class="bab-dek">Pada sejumlah emiten terbesar, hampir seluruh saham dipegang segelintir pengendali; ambang free float Jakarta termasuk paling rendah di antara bursa besar.</p>
            </div>
          </header>
          <FloatKonsentrasi client:visible />

          <div class="ledger lingk-absen absen-satu">
            <div><span><b class="absen-k mono">YANG TIDAK DICETAK DI SINI · MENUNGGU SUMBER</b> Bursa &amp; rupiah harian, diukur dari luar <span class="mono">deret dicetak dari baris data sungguhan, bukan simulasi — esainya menunggu di rak arsip perspektif</span></span><span class="v num">—</span></div>
          </div>
        </section>



        {/* RAK 09 · DUNIA: where the republic ranks, seen from outside.
             The two comparison essays retired to the arsip-perspektif
             shelf (wave 9b) — permalinks live on; one instrument here. */}
        <section class="bab" id="dunia">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">10</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>TEMA · DARI LUAR</span>
              <h2 class="bab-tajuk display">Dunia</h2>
              <p class="bab-dek">Di mana republik ini berdiri di antara bangsa lain — angka peringkat yang hanya tampak dari luar.</p>
            </div>
          </header>

          <div class="dunia-board">
            {DUNIA.map((d) => (
              <div class="dunia-row" data-rise>
                <p class={`dunia-n display num ${d.baik ? 'baik' : 'buruk'}`}>{d.nilai}</p>
                <div class="dunia-meta">
                  <p class="dunia-label">{d.label}</p>
                  <p class="dunia-peer mono">{d.peer}</p>
                </div>
                <button class="chip"><span class="tick">⊙</span>{d.chip}</button>
              </div>
            ))}
          </div>

        </section>

        <!-- RAK 10 · LINGKUNGAN & EKSTRAKSI -->
        <section class="bab" id="lingkungan">
          <header class="bab-kepala">
            <span class="bab-folio ghost-num" aria-hidden="true">11</span>
            <div class="bab-kepala-teks">
              <span class="inkbar"><span class="dot">●</span>TEMA · SUMBER DAYA ALAM</span>
              <h2 class="bab-tajuk display">Sumber Daya Alam</h2>
              <p class="bab-dek">Tanah yang menanggung pertumbuhan: hutan yang ditebang, dan — menyusul — siapa yang tergusur karenanya. Dokumen berdampingan, pembaca menyimpulkan.</p>
            </div>
          </header>

          <!-- retired (2026-07-03, §13.15+§13.17): Riwayat Udara Jakarta (the
               MAP owns live air), Janji yang Belum Cair (buku besar row), Sisa
               di Alam Liar (Act III Daftar Merah), and the SEEDED PetaPiksel
               heatmap. Its replacement is REAL: 24 years of Hansen/UMD loss
               via the keyless GFW stats route, harvested by
               scripts/fetch-hutan.mjs, per-province sums reconciling to the
               national total within 1 ha. Displacement stays an ABSENCE until
               its sources pass the verification desk. -->
          <div class="lingk-sub bab-sub" id="hutan" data-rise>
            <h3 class="bab-subjudul display">Hutan yang Hilang</h3>
            <p class="bab-kalimat fig">Dua puluh empat tahun kehilangan tutupan pohon, satu kolom per tahun; bagian gelap terjadi di dalam konsesi. Pilih provinsi di peta atau lensa dan serinya bertukar.</p>
            <HutanHilang client:visible />
          </div>

          <div class="lingk-sub bab-sub">
            <span class="eyebrow">DOKUMEN YANG SUDAH TERBUKA · DI PETA KABAR</span>
            <p class="bab-kalimat fig">Konsesi hutan dan sawit (1.040 poligon, KLHK), izin usaha pertambangan (4.797 wilayah, ESDM), dan titik api satelit hari ini: dokumen aslinya terbuka di lapisan peta.</p>
            <div class="lingk-chips">
              <button class="chip" data-buka-peta="konsesi"><span class="tick">◌</span>BUKA DI PETA · KONSESI HUTAN/SAWIT</button>
              <button class="chip" data-buka-peta="tambang"><span class="tick">◌</span>BUKA DI PETA · TAMBANG IUP</button>
              <button class="chip" data-buka-peta="kebakaran"><span class="tick">◌</span>BUKA DI PETA · TITIK API</button>
            </div>
          </div>

          <div class="lingk-sub bab-sub">
            <div class="ledger lingk-absen absen-satu">
              <div><span><b class="absen-k mono">YANG BELUM DICETAK · MENUNGGU VERIFIKASI</b> Warga tergusur konflik agraria, per tahun <span class="mono">kpa catahu · komnas ham · idmc — angka paling sensitif di koran ini; dicetak setelah lolos periksa sumber, bukan sebelum</span></span><span class="v num">—</span></div>
            </div>
            <button class="chip" data-buka-peta="udara"><span class="tick">◌</span>BUKA DI PETA · LAPISAN UDARA</button>
          </div>
        </section>

        <!-- archive: the perspektif permalinks live on; the shelf is one link away -->
        <!-- the closing register, not a chapter: rows official statistics do
             not carry. Per-chapter absences print in place (see Danantara). -->
        <section class="hening-blok" id="hening" aria-label="Yang tidak dihitung">
          <span class="eyebrow ganda-eyebrow">CATATAN · YANG TIDAK DIHITUNG · BARIS TANPA STATISTIK RESMI, DITAGIH TIAP EDISI</span>
          <div class="ledger hening-ledger">
            {KEHENINGAN.baris.map((b) => (
              <div>
                <span>{b.k} <span class="hening-sumber mono">{b.sumber}</span></span>
                <span class="v num">{b.v}</span>
              </div>
            ))}
            {KEHENINGAN.absen.map((a, i) => (
              <div class="hening-absen">
                <span>{a.k} <span class="hening-sumber mono">{a.catatan}</span></span>
                {i === 0
                  ? <span class="v num" data-annotate="circle" data-annotate-color="#c96a5a">{a.v}</span>
                  : <span class="v num">{a.v}</span>}
              </div>
            ))}
            <div class="hening-lanec">
              <span>{KEHENINGAN.laneC.teks}</span>
              <button class="chip lane-c">{KEHENINGAN.laneC.chip}</button>
            </div>
          </div>
          <p class="hening-foot mono">SATU WILAYAH ATAU TEMA DISOROT TIAP EDISI · USULKAN BARIS KOSONG LAIN DI GITHUB</p>
        </section>

        <!-- the act's last note: what the machine's numbers cost, in memoriam -->
        <ObituariHutan />

        <section class="arsip-teaser" aria-label="Arsip perspektif">
          <div class="arsip-head">
            <span class="inkbar"><span class="dot">●</span>ARSIP PERSPEKTIF</span>
            <a class="mono ink-link arsip-semua" href="/arsip">SETIAP LEMBAR PERNAH JADI HALAMAN MUKA · RAK LENGKAP →</a>
          </div>
        </section>

      </div>
    </section>

    <!-- ============ SEAM II · fajar arsip ============ -->
    <div class="seam" data-seam="mesin-atlas" style="--from:#100f0d;--to:#ece2cb" aria-hidden="true">
      <div class="slats">
        {Array.from({ length: 8 }).map(() => (
          <div class="slat"><i class="f"></i><i class="b"></i></div>
        ))}
      </div>
      <p class="seam-kicker mono">↓ NUSANTARA · TANAH, HAYATI, DAN RUPA</p>
    </div>

    <!-- ============ ACT III · NUSANTARA ============ -->
    <section class="act act-atlas" data-act="atlas" data-register="atlas" id="nusantara">
      <div class="tex tex-hatch" aria-hidden="true"></div>
      <div class="wrap">
        <h2 class="sect-title-atlas display reveal-glyphs">Nusantara</h2>
        <p class="atlas-sub fig">Tanah, hayati, dan rupa: sejarah, alam, dan manusia kepulauan, berganti tiap terbit.</p>

        <!-- the atlas plate: the archipelago itself, engraved from real polygons -->
        <figure class="plat-wrap">
          <PetaAtlas client:visible />
          <figcaption class="plat-cap mono">PLAT I · KEPULAUAN, DIGRAFIR DARI POLIGON RUPABUMI</figcaption>
        </figure>

        <!-- MANUSIA: the day's people + their tongues -->
        <header class="bab-kepala" id="manusia">
          <span class="bab-folio ghost-num" aria-hidden="true">I</span>
          <div class="bab-kepala-teks">
            <!-- §13.17 B.4: the LAMPIRAN label carries the pipeline name;
                 the SUBJECT below (the day's people) is the only display title -->
            <span class="inkbar"><span class="dot">●</span>LAMPIRAN I · MANUSIA</span>
            <p class="bab-dek">Satu dari ratusan suku kepulauan, berganti dua kali sehari, dan cara mereka berbicara.</p>
          </div>
        </header>
        <WajahNusantara client:visible />
        <Bahasa718 client:visible />

        <!-- HAYATI & EKOSISTEM: the Wallace/Weber lines as the control
             (§13.17 B.5). ZonaHayati absorbs the retired Daftar Merah (species
             ledger) and PetaRupa (the standalone Wallace band). -->
        <header class="bab-kepala" id="hayati">
          <span class="bab-folio ghost-num" aria-hidden="true">II</span>
          <div class="bab-kepala-teks">
            <span class="inkbar"><span class="dot">●</span>LAMPIRAN II · HAYATI &amp; EKOSISTEM</span>
            <p class="bab-dek">Garis Wallace dan Weber membelah kepulauan jadi tiga dunia satwa. Ketuk sebuah sisi; daftar spesiesnya terbuka, diurutkan dari yang paling terancam.</p>
          </div>
        </header>
        <ZonaHayati client:visible />
        <!-- the volcano note retired 2026-07-11 (one owner: the map's MAGMA
             layer carries the field; the fact reads there, with live status) -->

        <!-- RUPA & INGATAN: the archive register — a painting and a date share
             one plate (merged 2026-07-11): what the islands made, and what
             happened on this day, both quoted from their sources. -->
        <header class="bab-kepala" id="rupa">
          <span class="bab-folio ghost-num" aria-hidden="true">III</span>
          <div class="bab-kepala-teks">
            <span class="inkbar"><span class="dot">●</span>LAMPIRAN III · RUPA &amp; INGATAN</span>
            <p class="bab-dek">Satu lukisan dari khazanah Nusantara dan satu peristiwa dari hari ini dalam sejarah, masing-masing dengan keterangan dan tautan ke sumbernya.</p>
          </div>
        </header>
        <GaleriNusantara client:visible />
        <IngatanHari client:visible />

        <!-- PUSTAKA & PIKIRAN: the shelf — writers & public thinkers -->
        <header class="bab-kepala" id="pustaka">
          <span class="bab-folio ghost-num" aria-hidden="true">IV</span>
          <div class="bab-kepala-teks">
            <span class="inkbar"><span class="dot">●</span>LAMPIRAN IV · PUSTAKA &amp; PIKIRAN</span>
            <p class="bab-dek">Sepuluh pena di satu rak: penulis dan pemikir kepulauan, satu buku terambil tiap hari.</p>
          </div>
        </header>
        <PustakaPikiran client:visible />

        <!-- ALMANAK: the sciences with an Indonesian address -->
        <header class="bab-kepala" id="almanak">
          <span class="bab-folio ghost-num" aria-hidden="true">V</span>
          <div class="bab-kepala-teks">
            <span class="inkbar"><span class="dot">●</span>LAMPIRAN V · ALMANAK</span>
            <p class="bab-dek">Empat rak pengetahuan berganti tiap terbit: langit khatulistiwa, bumi yang bergerak, hayati kepulauan, dan aritmetika alam.</p>
          </div>
        </header>
        <AlmanakSains client:visible />

        <!-- TULISAN: the essay surface the newsroom writes into — renders
             ONLY when the edition carries tulisan[] (no empty frames) -->
        <TulisanPapan client:visible />
      </div>

      <!-- the living understory closes the act: after the record, what stays moving -->
      <RimbaHidup client:visible />
    </section>

    <!-- ============ RUANG MAIN ============ -->
    <section class="ruang-main" id="ruang-main" data-register="atlas" aria-label="Ruang main">
      <div class="wrap">
        <header class="bab-kepala main-kepala">
          <span class="bab-folio ghost-num" aria-hidden="true">◻</span>
          <div class="bab-kepala-teks">
            <span class="inkbar"><span class="dot">●</span>RUANG MAIN · TEKA-TEKI YANG SAMA UNTUK SEMUA PEMBACA</span>
            <p class="bab-dek">Dua permainan dari arsip edisi, satu cermin statistik yang tak mengirim apa-apa, dan satu kanvas bersama. Semua pembaca mendapat soal yang sama; skor dan petakmu tinggal di perangkat ini.</p>
          </div>
        </header>
        <div class="main-rak">
          <div class="main-sel"><TebakDaerah client:visible /></div>
          <div class="main-sel"><BenarSalah client:visible /></div>
          <div class="main-sel"><SensusDiri client:visible /></div>
          <div class="main-sel"><TuguRakyat client:visible /></div>
        </div>
      </div>
    </section>

    <!-- ============ COLOPHON ============ -->
    <footer class="kolofon" data-register="atlas">
      <div class="wrap">
        <div class="rule2" aria-hidden="true"></div>
        <div class="kolofon-grid">
          <div>
            <p class="kolofon-mast display">DETAK DETIK</p>
            <p class="kolofon-tag fig">Koran sipil yang dicetak mesin, diperiksa hukum, dan menyimpan ingatan.</p>
          </div>
          <div class="agate">
            <b>PEDOMAN REDAKSI</b><br />
            Setiap klaim ditautkan ke sumber primernya, atau tidak diterbitkan.
            Berita luar disajikan apa adanya; sintesis mesin selalu ditandai.
            Redaksi menyajikan dokumen, bukan tuduhan. Konten terbuka tanpa akun,
            dan data pembaca tidak pernah meninggalkan perangkat.<br />
            <a class="ink-link" href="/pedoman">Pedoman selengkapnya →</a>
          </div>
          <div class="agate">
            <b>IRAMA DATA</b><br />
            Putusan · harian<br />
            Peringatan hutan · harian<br />
            Harga pangan · harian<br />
            APBD · triwulanan<br />
            Janji · dinilai tiap terbit<br />
            Ralat · transparan, selalu
          </div>
          <div class="agate">
            <b>KOLOFON</b><br />
            Astro 7 · Svelte 5 · MapLibre<br />
            Peta: OpenFreeMap · OSM<br />
            Aksara: Fraunces · Archivo · Geist Mono<br />
            {lajurHost ? (
              <Fragment>Lajur langsung: <b>{lajurHost}</b><br /></Fragment>
            ) : (
              <Fragment>Lajur: <b>mode contoh</b> · AKSARA_URL belum masuk build<br /></Fragment>
            )}
            <a class="ink-link" href="https://github.com/yohn-maistre/detak-detik">github.com/yohn-maistre/detak-detik</a>
          </div>
        </div>
        <p class="kolofon-bawah mono">
          DD/EDI/041/VI/2026 · DICETAK OTOMATIS 05.00 WIB · <a class="ink-link" href="/sumber">SUMBER & METODE: SEMUA KUITANSI DI SATU LACI →</a> · <a class="ink-link" href="/arsip">ARSIP PERSPEKTIF</a> · ⌘ TIDAK ADA PELACAK, TIDAK ADA PROFIL
        </p>
      </div>
    </footer>
  </main>

  <!-- the page curl, repurposed: the corner unfolds into the daftar isi
       (navigation TOC); the refresh countdown rides its footer -->
  <DaftarIsi client:idle />

  <Aksara client:load />

  <script>
    import { boot } from '../scripts/choreo';
    import '../scripts/pagi-live';
    boot();
  </script>
</Paper>

<style>
  main { overflow-x: clip; }
  /* acts own their registers statically; each paints its own paper */
  .act, .kolofon { position: relative; background: var(--bg); color: var(--ink); }
  .act { padding: 0 0 clamp(56px, 9vh, 96px); }

  /* ---------- ACT I ---------- */
  .masthead-block { padding-top: clamp(20px, 4vh, 44px); }
  .folio-top {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 24px;
    justify-content: space-between;
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--muted);
    padding: 10px 0;
    border-bottom: 1px solid var(--line);
  }
  .detak { background: none; border: none; cursor: pointer; color: var(--accent); font-size: 10px; letter-spacing: 0.16em; }
  .jam-tri { display: flex; gap: 14px; flex-wrap: wrap; align-items: center; }
  /* clockwork: a second-hand that ticks once per second — the paper's pulse */
  .detik { display: inline-flex; align-items: center; }
  .detik svg { width: 22px; height: 22px; display: block; }
  .detik-rim { fill: none; stroke: var(--line); stroke-width: 0.5; opacity: 0.4; }
  .detik-min { stroke: var(--muted); stroke-width: 0.4; opacity: 0.45; }
  .detik-maj { stroke: var(--ink); stroke-width: 0.7; opacity: 0.7; }
  .detik-hub { fill: var(--accent); }
  .detik-hand { stroke: var(--accent); stroke-width: 1.1; stroke-linecap: round; transform-origin: 20px 20px; animation: detik 60s steps(60, end) infinite; }
  @keyframes detik { to { transform: rotate(360deg); } }
  /* KILAS heartbeat */
  .pulse-dot { animation: hb 2.6s ease-in-out infinite; transform-origin: center; }
  @keyframes hb { 0%, 100% { transform: scale(1); opacity: 1; } 10% { transform: scale(1.55); } 20% { transform: scale(1); } 30% { transform: scale(1.4); opacity: 0.8; } 42% { transform: scale(1); opacity: 1; } }
  @media (prefers-reduced-motion: reduce) { .detik-hand, .pulse-dot { animation: none; } }
  .jam-z { color: var(--muted); }
  .jam-z b { color: var(--accent); font-weight: 500; font-variant-numeric: tabular-nums; }
  @media (max-width: 760px) { .jam-tri { gap: 10px; } }

  .masthead-grid {
    display: grid;
    grid-template-columns: 1fr auto;
    gap: clamp(16px, 4vw, 56px);
    align-items: stretch;
    margin: 22px 0 12px;
  }
  @media (max-width: 860px) { .masthead-grid { grid-template-columns: 1fr; } }

  .masthead {
    font-size: clamp(64px, 12.5vw, 196px);
    line-height: 0.86;
    letter-spacing: -0.03em;
    overflow: hidden;
  }
  .mast-line { display: block; white-space: nowrap; }
  .masthead :global(.glyph) { display: inline-block; will-change: transform; }
  .masthead :global(.glyph:hover) { color: var(--accent); transition: color 0.2s; }

  .infobox {
    align-self: end;
    border: 1px solid var(--line);
    padding: 14px 16px 12px;
    min-width: 218px;
    font-size: 10px;
    letter-spacing: 0.1em;
    color: var(--muted);
  }
  @media (max-width: 860px) { .infobox { display: none; } }
  .infobox-row {
    display: flex;
    justify-content: space-between;
    gap: 18px;
    padding: 4px 0;
    border-bottom: 1px dashed var(--line-soft);
  }
  .infobox-row b { color: var(--ink); font-weight: 600; }
  .infobox-row b.num { color: var(--accent); }
  /* the live-lane telltale: an unset PUBLIC_AKSARA_URL must never be silent */
  .infobox-row b.lajur-live { color: var(--accent); }
  .infobox-row b.lajur-contoh { color: var(--muted); }
  .barcode {
    height: 30px;
    margin: 12px 0 8px;
    background: repeating-linear-gradient(
      90deg,
      var(--ink) 0 2px, transparent 2px 4px,
      var(--ink) 4px 7px, transparent 7px 9px,
      var(--ink) 9px 10px, transparent 10px 14px,
      var(--ink) 14px 16px, transparent 16px 17px,
      var(--ink) 17px 21px, transparent 21px 24px
    );
  }
  .infobox-foot { font-size: 8.5px; letter-spacing: 0.2em; line-height: 1.7; }

  .folio {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 22px;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
    margin: 10px 0 14px;
  }
  .folio b { color: var(--accent); font-weight: 600; }
  .fajar-btn { background: none; border: none; color: var(--muted); cursor: pointer; font-size: inherit; letter-spacing: inherit; padding: 0; transition: color 0.25s; }
  .fajar-btn:hover { color: var(--accent); }

  .peta-cuaca { border-bottom: 1px solid var(--line-soft); margin-bottom: 14px; }

  .kabar-head { display: flex; align-items: center; gap: 18px; flex-wrap: wrap; margin-bottom: 14px; }
  /* the kitchen-table series continues the PASAR section as an open movement */
  .gelombang-blok { border-top: 1px solid var(--line-soft); padding-top: 24px; margin-top: 30px; margin-bottom: 40px; }

  /* Peta Kabar — the Act I → Act II bridge (live national layers + dossier) */
  .peta-blok { margin-top: 34px; }

  /* chapter sheets: one rhythm below the band + scoreboard, everywhere */
  .bab-lembar { margin-top: clamp(28px, 4.5vw, 44px); }
  .bab-lembar.daerah-lensa { margin-bottom: clamp(28px, 4.5vw, 44px); }
  /* inlined perspektif sub-piece inside a chapter */
  .bab-sub { margin-top: 42px; border-top: 1px solid var(--line); padding-top: 28px; }
  .bab-subjudul { font-family: 'Fraunces Variable', serif; font-weight: 360; font-size: clamp(24px, 3.6vw, 40px); line-height: 1.05; margin-bottom: 4px; }

  /* Dunia scoreboard: big numerals on ruled rows, not tiles */
  .dunia-board { border-top: 1px solid var(--line); margin-bottom: 10px; }
  .dunia-row { display: grid; grid-template-columns: minmax(96px, 0.46fr) 1fr auto; gap: clamp(14px, 3vw, 34px); align-items: center; padding: 18px 2px; border-bottom: 1px solid var(--line); }
  @media (max-width: 640px) { .dunia-row { grid-template-columns: minmax(72px, auto) 1fr; } .dunia-row .chip { grid-column: 2; justify-self: start; } }
  .dunia-n { font-family: 'Fraunces Variable', serif; font-weight: 320; font-size: clamp(36px, 6vw, 66px); line-height: 0.88; letter-spacing: -0.01em; }
  .dunia-n.baik { color: var(--accent2); }
  .dunia-n.buruk { color: var(--ink); }
  .dunia-meta { display: grid; gap: 4px; }
  .dunia-row .dunia-label { font-size: clamp(14px, 1.8vw, 17px); color: var(--ink); flex: none; }
  .dunia-peer { font-size: 10px; letter-spacing: 0.04em; color: var(--muted); }
  .dunia-row .chip { align-self: center; }

  /* ---- movement dividers (Act II spine: two GERAK) ---- */
  .gerak { border-top: 2px solid var(--line); padding-top: clamp(18px, 3.5vw, 36px); margin-top: clamp(18px, 4.5vw, 48px); }
  .gerak-no { font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.42em; color: var(--accent2); display: block; margin-bottom: 6px; }
  .gerak-judul { font-family: 'Fraunces Variable', serif; font-weight: 300; font-size: clamp(56px, 13vw, 140px); line-height: 0.84; margin: 0 0 14px; letter-spacing: -0.02em; color: var(--accent2); }
  .gerak-dek { font-size: clamp(15px, 1.9vw, 20px); color: var(--muted); max-width: 60ch; line-height: 1.5; }

  /* ---- BAB chapter headers (Act II) ---- */
  .bab { padding-top: 8px; }
  .bab-head { margin-bottom: 22px; }
  .bab-dek { font-size: clamp(14px, 1.4vw, 16px); color: var(--muted); margin-top: 12px; max-width: 58ch; line-height: 1.55; }
  /* the branch's constitutional mandate, quoted verbatim: what it is FOR,
     so every reading below is against the branch's own charter */
  .bab-fungsi { margin-top: 14px; max-width: 64ch; line-height: 1.5; }
  .bab-fungsi .fig { font-style: italic; font-size: clamp(13.5px, 1.5vw, 16px); color: var(--ink); }
  .bab-fungsi .mono { font-size: 8.5px; letter-spacing: 0.14em; color: var(--muted); white-space: nowrap; margin-left: 8px; }

  /* the chapter masthead: a stroked folio numeral + small kicker + big serif title */
  /* one chapter-header anatomy, everywhere: kicker, title, dek, all flush
     left; the folio number is quiet page furniture at the top right. */
  .bab-kepala { position: relative; display: block; border-top: 2px solid var(--line); padding-top: 18px; margin-bottom: clamp(22px, 3.4vw, 34px); }
  .bab-folio { position: absolute; top: 16px; right: 0; font-size: clamp(22px, 2.4vw, 30px); line-height: 1; margin: 0; }
  .bab-kepala-teks { min-width: 0; }
  .bab-tajuk { font-family: 'Fraunces Variable', serif; font-weight: 340; font-size: var(--fs-5); line-height: 1; letter-spacing: -0.012em; color: var(--ink); margin: 11px 0 0; }
  .bab-kepala .inkbar { margin-bottom: 2px; }
  .bab-jeda { grid-template-columns: 1fr; border-top-style: dashed; }
  @media (max-width: 680px) { .bab-folio { top: 20px; font-size: 18px; } }

  /* the five-branch index strip (bands now live in their chapters) */
  .cabang-indeks { border-top: 1px solid var(--line); }
  .hari-ini { display: grid; gap: 12px; margin-top: clamp(28px, 5vh, 52px); }
  .ci-head { display: block; font-size: 9px; letter-spacing: 0.2em; color: var(--accent2); padding: 12px 2px 4px; }
  .ci-head .dot { color: var(--accent); margin-right: 8px; }
  .ci-list { list-style: none; display: grid; margin: 0; padding: 0; }
  .ci-item { border-top: 1px solid var(--line-soft); }
  .ci-item a { display: grid; grid-template-columns: auto auto 1fr auto; align-items: baseline; gap: clamp(12px, 2vw, 26px); padding: 14px 2px; text-decoration: none; color: var(--ink); transition: padding-left 0.25s var(--ease-out), background 0.2s; }
  .ci-item a:hover { padding-left: 14px; background: color-mix(in oklab, var(--accent2) 9%, transparent); }
  .ci-no { font-size: 10px; letter-spacing: 0.14em; color: var(--accent); }
  .ci-nama { font-family: 'Fraunces Variable', serif; font-weight: 360; font-size: clamp(20px, 2.6vw, 30px); line-height: 1; }
  .ci-ringkas { font-size: 12.5px; color: var(--muted); }
  .ci-panah { color: var(--accent2); font-size: 14px; transition: transform 0.25s var(--ease-out); }
  .ci-item a:hover .ci-panah { transform: translateY(3px); }
  @media (max-width: 680px) { .ci-item a { grid-template-columns: auto 1fr auto; } .ci-ringkas { display: none; } }
  .bab-judul { font-family: 'Fraunces Variable', serif; font-weight: 360; font-size: clamp(28px, 4.4vw, 52px); line-height: 1.04; }
  .bab-kalimat { font-size: clamp(15px, 1.8vw, 18px); margin: 8px 0 22px; max-width: 56ch; }

  /* ---- Aparat: open ledger columns — boxes are for artifacts ---- */
  .aparat-grid { display: grid; grid-template-columns: 1fr 1.6fr; gap: clamp(22px, 4vw, 48px); align-items: start; }
  @media (max-width: 800px) { .aparat-grid { grid-template-columns: 1fr; } }
  .aparat-col { border-top: 2px solid var(--line); padding-top: 14px; display: flex; flex-direction: column; gap: 10px; }
  .aparat-row { display: flex; justify-content: space-between; gap: 12px; border-bottom: 1px solid var(--line-soft); padding: 7px 0; font-size: 13px; }
  .aparat-row b { color: var(--accent); }
  .aparat-angka { font-family: 'Fraunces Variable', serif; font-weight: 340; font-size: clamp(40px, 6vw, 72px); line-height: 1; }
  .aparat-angka.accent { color: var(--accent); }
  .aparat-note { font-size: 14px; color: var(--muted); line-height: 1.55; }
  .aparat-impun { font-size: 13px; color: var(--muted); line-height: 1.6; border-left: 2px solid var(--line); padding-left: 12px; }
  .aparat-col .chip, .ekonomi-otsus .chip { align-self: flex-start; }

  /* ---- Ekonomi & Daerah ---- */
  .ekonomi-col { display: grid; gap: 22px; align-content: start; }
  .ekonomi-stat { display: grid; gap: 6px; }
  .ekonomi-otsus { display: grid; gap: 10px; border-top: 1px solid var(--line); padding-top: 18px; }
  .ekonomi-teks { font-size: 14.5px; line-height: 1.6; color: var(--muted); max-width: 52ch; }

  /* ---- Janji (now Bab VI) ---- */
  /* the buku besar: ruled ledger rows — entry № · promise · measurement · stamp */
  .janji-buku { border-top: 2px solid var(--ink); }

  /* ---- the scoreboard ---- */
  .cabang-blok { padding-top: 8px; }
  .cabang-grid { display: grid; grid-template-columns: repeat(5, 1fr); border: 1px solid var(--line); }
  @media (max-width: 980px) { .cabang-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 520px) { .cabang-grid { grid-template-columns: 1fr; } }
  .cabang-col { padding: 16px; border-right: 1px solid var(--line); border-bottom: 1px solid var(--line); background: var(--card); display: flex; flex-direction: column; gap: 10px; }
  .cabang-nama { font-size: 11px; letter-spacing: 0.18em; color: var(--accent); border-bottom: 2px solid var(--accent); padding-bottom: 6px; }
  .cabang-apa { font-size: 12px; color: var(--muted); line-height: 1.5; min-height: 4.6em; }
  .cabang-metrik { display: grid; gap: 5px; }
  .cabang-row { display: flex; justify-content: space-between; gap: 8px; font-size: 11px; border-bottom: 1px dashed var(--line-soft); padding-bottom: 4px; }
  .cabang-row b { color: var(--ink); white-space: nowrap; }
  .cabang-col .chip { align-self: flex-start; font-size: 8.5px; padding: 4px 7px; }

  /* ---- Act III + Ruang Main ---- */
  /* lampiran rhythm: each plate is its own sitting — a real chapter break
     above every lampiran header, not the tight in-act cadence of Act II */
  .act-atlas .bab-kepala { margin-top: clamp(64px, 10vw, 128px); margin-bottom: clamp(28px, 4.5vw, 44px); }
  /* the plate convention: engraved caption under every atlas figure (§13.14) */
  .plat-wrap { margin: 0; }
  .plat-cap { display: block; font-size: 8.5px; letter-spacing: 0.22em; color: var(--muted); text-align: center; margin-top: 10px; }
  #hutan { margin-top: 8px; }
  /* top space lives as PADDING, not the header's margin — a collapsed
     margin here would open a strip of body background between the acts */
  .ruang-main { background: var(--bg); color: var(--ink); padding: clamp(48px, 7vw, 88px) 0 90px; }
  .ruang-main .main-kepala { margin-top: 0; }
  /* the arcade spread: two columns, wide gutters, a quiet rule down the
     middle — each game is its own sitting, not a cramped shelf of three */
  .main-rak {
    display: grid; grid-template-columns: 1fr 1fr;
    gap: clamp(48px, 7vw, 84px) 0;
    margin-top: clamp(24px, 4vw, 40px); align-items: start;
  }
  .main-sel { min-width: 0; padding-right: clamp(28px, 4.5vw, 60px); }
  .main-sel:nth-child(2n) { border-left: 1px dashed var(--line-soft); padding-left: clamp(28px, 4.5vw, 60px); padding-right: 0; }
  @media (max-width: 880px) {
    .main-rak { grid-template-columns: 1fr; gap: 44px; }
    .main-sel { padding-right: 0; }
    .main-sel:nth-child(2n) { border-left: none; padding-left: 0; }
    /* stacked games get a quiet rule between them so each puzzle reads as its own sitting */
    .main-sel:not(:first-child) { border-top: 1px dashed var(--line-soft); padding-top: 36px; }
  }

  .angka-blok {
    position: relative;
    display: grid;
    grid-template-columns: 1.7fr 1px 1fr;
    gap: clamp(20px, 4vw, 52px);
    align-items: end;
    padding: clamp(40px, 7vh, 80px) 0 28px;
  }
  /* the veil paints at z 0; the figures ride above it */
  .angka-kiri, .angka-sela, .angka-kanan {
    position: relative;
    z-index: 1;
  }
  @media (max-width: 900px) {
    .angka-blok { grid-template-columns: 1fr; }
    .angka-sela { display: none; }
  }
  .angka-sela { background: var(--line); align-self: stretch; }
  .angka {
    font-size: clamp(44px, 8.4vw, 116px);
    line-height: 1;
    color: var(--accent);
    margin: 18px 0 10px;
    font-variant-numeric: tabular-nums lining-nums;
    user-select: none;
  }
  .angka-label { max-width: 44ch; color: var(--muted); font-size: 15px; }
  .cite-chips { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 16px; }
  .angka-denom { display: inline-flex; flex-wrap: wrap; align-items: stretch; border: 1px solid var(--line); margin-top: 16px; font-size: 9px; letter-spacing: 0.1em; }
  /* a caption, not a toggle: muted + un-filled so it never reads as a button */
  .angka-denom-label { padding: 6px 9px; background: transparent; color: var(--muted); font-style: italic; display: flex; align-items: center; }
  .angka-denom-btn { background: none; border: none; border-left: 1px solid var(--line); padding: 6px 10px; font: inherit; letter-spacing: inherit; color: var(--ink); cursor: pointer; transition: background 0.2s, color 0.2s; }
  .angka-denom-btn.aktif { background: var(--accent); color: var(--bg); }

  .sparkbars {
    display: flex;
    align-items: flex-end;
    gap: 5px;
    height: 110px;
    margin: 14px 0 10px;
    border-bottom: 2px solid var(--line);
    padding-bottom: 0;
  }
  .sparkbar {
    flex: 1;
    height: var(--h);
    background: var(--ink);
    position: relative;
    transition: background 0.25s;
    min-width: 6px;
  }
  .sparkbar:last-child { background: var(--accent); }
  .sparkbar:hover { background: var(--accent); }
  .sparkbar:hover::after {
    content: attr(data-v);
    position: absolute;
    top: -20px;
    left: 50%;
    translate: -50% 0;
    font-family: var(--font-mono);
    font-size: 10px;
    color: var(--ink);
  }
  .spark-foot { font-size: 10px; letter-spacing: 0.14em; color: var(--muted); }
  .spark-foot b { color: var(--ink); font-size: 12px; }
  .spark-max { color: var(--accent); }

  .pasar-blok { margin-top: 34px; }
  /* the findings board reads like a front page: one lead, then ruled rows.
     hierarchy is the ranking made visible, not six equal boxes. */
  .temuan-blok { padding: 32px 0 36px; border-top: 1px solid var(--line); margin-top: 40px; }
  .temuan-head-row { display: flex; align-items: center; gap: 18px; flex-wrap: wrap; }
  /* the findings board reads in the FEED's row grammar (rank numeral left,
     ruled rows, no featured first item — the front page lead owns that job) */
  .temuan-grid { display: grid; margin-top: 10px; border-top: 2px solid var(--ink); }
  .temuan {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    column-gap: clamp(14px, 2vw, 26px);
    row-gap: 7px;
    padding: 18px 0 20px;
    border-bottom: 1px solid var(--line);
  }
  .temuan .ghost-num { grid-row: 1 / span 4; font-size: 30px; line-height: 1; opacity: 0.4; padding-top: 3px; min-width: 40px; pointer-events: none; }
  .temuan-head { display: flex; gap: 12px; align-items: baseline; font-size: 10px; letter-spacing: 0.16em; color: var(--muted); }
  .temuan-lens {
    color: var(--bg);
    background: var(--accent);
    font-weight: 700;
    padding: 2px 7px 1px;
  }
  .temuan-headline { font-size: clamp(18px, 1.8vw, 23px); line-height: 1.16; max-width: 44ch; }
  .temuan-body { font-size: 13.5px; line-height: 1.55; color: var(--muted); max-width: 62ch; }
  .temuan-foot { display: flex; justify-content: space-between; align-items: flex-end; gap: 10px; flex-wrap: wrap; }
  .temuan-foot .cite-chips { margin: 0; }

  .hukum-grid { display: grid; grid-template-columns: 1.5fr 1fr; gap: 24px; align-items: start; }
  @media (max-width: 900px) { .hukum-grid { grid-template-columns: 1fr; } }
  .struk-rail { display: grid; justify-items: center; gap: 14px; }
  .struk-stamp { position: absolute; top: 12px; right: 12px; font-size: 9px; }
  .struk-sub { text-align: center; color: #6e675c; font-size: 11px; }
  .struk h4 { text-align: center; letter-spacing: 0.1em; font-size: 13px; }
  .figcap-dinas { font-size: 9px; letter-spacing: 0.2em; color: var(--muted); }

  /* ---------- SEAMS: the compact ribbon flip ---------- */
  .seam {
    position: relative;
    height: 20vh;
    height: 20svh;
    max-height: 220px;
    display: grid;
    place-items: center;
    perspective: 1400px;
    background: var(--to);
  }
  @media (max-width: 760px) { .seam { height: 18vh; height: 18svh; } }
  .seam.is-static { height: clamp(120px, 20vh, 220px); background: linear-gradient(var(--from), var(--to)); }
  .slats { position: absolute; inset: 0; display: grid; grid-auto-rows: 1fr; }
  .slat { position: relative; transform-style: preserve-3d; will-change: transform; }
  .slat i { position: absolute; inset: -0.5px 0; backface-visibility: hidden; }
  .slat .f { background: var(--from); }
  .slat .b { background: var(--to); transform: rotateX(180deg); }
  .seam.is-static .slats { display: none; }
  /* a tiny kicker rides above the flip, legible on either paper via blend */
  .seam-kicker {
    position: relative;
    z-index: 2;
    text-align: center;
    padding: 0 24px;
    color: #8d8676;
    mix-blend-mode: difference;
    font-size: 9px;
    letter-spacing: 0.26em;
  }

  /* ---------- ACT II ---------- */
  .act-mesin .wrap { display: grid; gap: clamp(48px, 9vh, 110px); }
  .hening-blok { margin: clamp(36px, 6vw, 64px) 0 8px; }
  .hening-ledger > div { padding: 9px 0; }
  .lingk-chips { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 10px; }
  .lingk-absen { margin: 8px 0 12px; }
  .lingk-absen .mono { font-size: 9px; letter-spacing: 0.08em; color: var(--muted); }
  /* one-line absence: the ruled row IS the section — no lone eyebrow floating
     over a single dash (it read as an unfinished chapter) */
  .absen-satu { margin-top: 30px; }
  .absen-satu .absen-k { font-weight: 500; font-size: 9px; letter-spacing: 0.14em; color: var(--accent2); margin-right: 8px; }
  .lingk-catatan { max-width: 62ch; }
  .lingk-sub .eyebrow { display: block; margin-bottom: 8px; }
  .hening-sumber { font-size: 9.5px; letter-spacing: 0.12em; color: var(--muted); margin-left: 10px; text-transform: uppercase; }
  .hening-absen {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    padding: 9px 0;
    border-bottom: 1px solid var(--line);
    color: #c96a5a;
  }
  .hening-absen .hening-sumber { color: #c96a5a; font-style: italic; }
  .hening-foot { margin-top: 14px; font-size: 9px; letter-spacing: 0.18em; color: var(--muted); }
  .hening-lanec {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    align-items: center;
    padding: 12px 0 9px 22px;
    border-bottom: none !important;
    font-size: 14px;
    color: var(--muted);
  }

  .ganda-eyebrow { margin-bottom: 18px; }

  .arsip-head { display: flex; justify-content: space-between; align-items: baseline; gap: 16px; flex-wrap: wrap; }
  .arsip-semua { font-size: 10px; letter-spacing: 0.18em; color: var(--accent2); }

  /* ---------- ACT III ---------- */
  .act-atlas { padding-top: 10px; }
  .sect-title-atlas {
    font-family: 'Fraunces Variable', serif;
    font-weight: 460;
    font-size: clamp(38px, 6.5vw, 92px);
    margin-bottom: 8px;
  }
  .atlas-sub { font-size: 15px; margin-bottom: 26px; }
  /* Act III section dividers (atlas register) */
  /* .nus-sec* / .atlas-grid removed 2026-07-11: vestigial from an earlier
     Act III layout, superseded by .bab-kepala. */

  /* tarik fajar */
  #fajar {
    position: relative;
    height: 0;
    overflow: hidden;
    background: #100f0d;
  }
  #fajar canvas { display: block; width: 100%; height: 200px; }
  .fajar-label {
    position: absolute;
    left: 50%;
    bottom: 10px;
    transform: translateX(-50%);
    font-size: 8.5px;
    letter-spacing: 0.26em;
    color: #cdb47a;
    white-space: nowrap;
  }

  .janji {
    display: grid; grid-template-columns: auto minmax(0, 1.1fr) minmax(0, 1fr) auto;
    gap: 8px 28px; align-items: start; padding: 16px 0 18px;
    border-bottom: 1px solid var(--line);
  }
  .janji-no { font-size: 19px; line-height: 1.1; opacity: 0.4; padding-top: 2px; min-width: 26px; }
  .janji-isi { display: grid; gap: 6px; }
  .janji-ukur { display: grid; gap: 8px; align-self: center; }
  /* the grade, drawn: target tick vs measured dot on one track (SkorCabang's
     dumbbell vocabulary); a hatched track is a missing measurement */
  .janji-track { position: relative; height: 12px; margin-top: 10px; }
  .janji-track::before { content: ''; position: absolute; left: 0; right: 0; top: 50%; height: 1px; background: var(--line-soft); }
  .jt-target { position: absolute; top: 0; bottom: 0; width: 2px; background: var(--accent2); transform: translateX(-50%); }
  .jt-bar { position: absolute; top: 50%; height: 2px; transform: translateY(-50%); background: var(--muted); }
  .jt-real { position: absolute; top: 50%; width: 9px; height: 9px; border-radius: 50%; transform: translate(-50%, -50%); background: var(--ink); }
  .janji-track.gagal .jt-real { background: var(--accent); }
  .janji-track.gagal .jt-bar { background: var(--accent); opacity: 0.6; }
  .janji-track.kosong::before { background: repeating-linear-gradient(90deg, var(--line) 0 5px, transparent 5px 10px); }
  /* right padding reserves the stamp's corner so long promises never collide */
  .janji-teks { font-size: clamp(15px, 1.7vw, 18px); max-width: 44ch; line-height: 1.4; }
  .janji-meta { font-size: 9.5px; letter-spacing: 0.12em; color: var(--muted); text-transform: uppercase; }
  .janji-baris { display: flex; flex-wrap: wrap; gap: 6px 22px; font-size: 11px; color: var(--muted); }
  .janji-stamp { justify-self: end; align-self: start; font-size: 9px; }
  /* stamp ink follows the track's own verdict: missed = madder, met = calm ink,
     running = quiet, absent measurement = dashed absence */
  .janji-stamp.s-tercapai { color: var(--accent2); border-color: var(--accent2); }
  .janji-stamp.s-berjalan { color: var(--muted); border-color: var(--line); }
  .janji-stamp.s-tidak-tercapai { color: var(--accent); border-color: var(--accent); }
  .janji-stamp.s-data-tidak-tersedia { color: var(--muted); border-color: var(--muted); border-style: dashed; }
  @media (max-width: 800px) {
    .janji { grid-template-columns: auto 1fr; }
    .janji-ukur, .janji-stamp { grid-column: 2; }
    .janji-stamp { justify-self: start; }
  }
  .janji-foot { margin-top: 16px; font-size: 9px; letter-spacing: 0.18em; color: var(--muted); }

  /* ---------- colophon ---------- */
  .kolofon { padding: 40px 0 110px; }
  .kolofon-grid { display: grid; grid-template-columns: 1.4fr 1fr 1fr 1fr; gap: 28px; padding: 30px 0; }
  @media (max-width: 940px) { .kolofon-grid { grid-template-columns: 1fr 1fr; } }
  @media (max-width: 560px) { .kolofon-grid { grid-template-columns: 1fr; } }
  .kolofon-mast { font-size: clamp(26px, 3.4vw, 40px); }
  .kolofon-tag { font-size: 15px; margin-top: 8px; max-width: 30ch; }
  .agate b { color: var(--ink); letter-spacing: 0.14em; }
  .kolofon-bawah { font-size: 9.5px; letter-spacing: 0.16em; color: var(--muted); border-top: 1px solid var(--line); padding-top: 14px; }

  /* page curl → DaftarIsi island (the corner is navigation now) */
</style>
